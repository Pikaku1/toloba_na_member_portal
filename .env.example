# Member portal auth + member-owned data (`members`, `hub_contributions`) — import your ITS roster into THIS deployment.
VITE_CONVEX_URL_MEMBER=https://mild-hedgehog-2.convex.cloud

# Optional: read admin-owned tables from a different deployment when split (announcements, surveys, hub collections).
VITE_CONVEX_URL_ADMIN=https://fortunate-ox-402.convex.cloud